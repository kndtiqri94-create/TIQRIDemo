# Definition of Done — User Story 1.1 Job Application Frontend

- **Story:** `docs/user-stories/1.1_JOB_APPLICATION_FRONTEND_USER_STORY.md`
- **Plan:** `docs/features/1.1_JOB_APPLICATION_FRONTEND_PLAN.md`
- **API Doc:** N/A — this story has no API (PRD Non-Goal NG1; Plan §5 confirms no `docs/api-docs/*` is produced).
- **Backend owner:** Harinda (Backend Developer) — N/A, zero backend tasks in this workstream (F-1.2 is separate/non-dispatched).
- **Frontend owner:** Hasika (Frontend Developer)
- **Status:** Ready for Review
- **Created:** 2026-08-13

Every box below MUST be ticked by the developer agent(s) responsible for each area. Any unticked box means the work is NOT Done and is NOT to be submitted for review. The Code Reviewer treats any unticked or unevidenced box as a blocking finding.

## Plan / Story coverage
| # | Item | Evidence |
|---|---|---|
| [x] **DoD.1** | Every task listed in the Plan `Files to Touch` section has been addressed (added/modified/deleted). | All files in Plan §3.3 created — see story File List; scaffold (§3.3 line 28) at `frontend-app/hireflow-web/`; `job-application-page.component.*`, `job-posting-details.component.*`, `application-form.component.*`, `cv-upload.component.*`, `application-confirmation.component.*`, `job-application.model.ts`, `cv-attachment.model.ts`, `job-application-storage.service.ts`, `cv-file-validation.util.ts`, `application-form-validators.util.ts`, `_tokens.scss`, `_breakpoints.scss`, `app.routes.ts` — all present under `frontend-app/hireflow-web/src/`. |
| [x] **DoD.2** | Every Acceptance Criterion (`AC-N`) from the user story is implemented. | AC-1: `job-posting-details.component.html:1-40`. AC-2: `shared/utils/dead-link.util.ts:1-6` used in `job-application-page.component.html:10,39-40` and `application-confirmation.component.html:9`. AC-3: `application-form.component.ts:37,57-60,70-72`, `application-form.component.html:60-68`. AC-4/AC-5: `cv-upload.component.ts:51-55,78-93`, `core/utils/cv-file-validation.util.ts:22-39`. AC-6: `application-form.component.ts:43-55,78-88`, `application-form.component.html:7-12`. AC-7: `application-form.component.ts:39-45` (computed re-evaluates on every signal write, no re-submit needed). AC-8: `job-application-page.component.ts:32-38`, `application-confirmation.component.html:1-12`. AC-9: `job-application-page.component.ts:46-49` (fresh `ApplicationFormComponent` instance via `@if`/`@else` swap in `job-application-page.component.html:24-31`). AC-10: `job-application-storage.service.ts:20-24,32-37`, surfaced via `job-application-page.component.ts:39-43` and rendered in `application-form.component.html:14-19`. AC-11: `job-application-page.component.scss:123-147` (grid ≥900px / gutters ≤420px). |
| [x] **DoD.3** | Story's `File List` section is complete and accurate (Added / Modified / Deleted). | See `docs/user-stories/1.1_JOB_APPLICATION_FRONTEND_USER_STORY.md` §9.5, updated in this iteration. |
| [x] **DoD.4** | Story's Change Log has a new entry for this iteration. | See `docs/user-stories/1.1_JOB_APPLICATION_FRONTEND_USER_STORY.md` §9.6. |

## Standards compliance
| # | Item | Evidence |
|---|---|---|
| [ ] **DoD.5** | Backend changes comply with `docs/development-standards/BACKEND_STANDARDS.md`. | N/A — no backend changes in this workstream (Plan §3.1/§3.2, §11). |
| [x] **DoD.6** | Frontend changes comply with `docs/development-standards/FRONTEND_STANDARDS.md` (loaded via `INDEX.md` for the concerns touched: `frontend.structure`, `frontend.components`, `frontend.lifecycle`, `frontend.component-files`, `frontend.styles`, `frontend.forms-a11y`, `frontend.services`, `frontend.models`, `frontend.uiux`, `frontend.a11y-*`, `frontend.testing`). | Standalone components with separate `.ts`/`.html`/`.scss`/`.spec.ts` files throughout `frontend-app/hireflow-web/src/app/features/job-application/**`; domain models under `core/models/domain`, shared model under `core/models/shared`, entity-agnostic service under `core/services`, pure helpers under `core/utils` and `shared/utils` per Model/Service Standards. |
| [x] **DoD.7** | Code follows the exemplars cited in the Plan (or states why not, with a replacement named). | Plan §2 explicitly states the `manageusers` CRUD exemplar does not fit this public, unauthenticated, client-storage-only form and directs a fresh Angular 18+ scaffold reusing only the FRONTEND_STANDARDS folder layout — followed: scaffolded latest stable Angular CLI 22.1.3 (`frontend-app/hireflow-web/angular.json:1` confirms `@angular/cli ^22.1.0`), used `app/core`, `app/shared`, `app/features` layout per FRONTEND_STANDARDS "Project Structure". File/naming conventions (kebab-case `*.component.ts`, PascalCase class names, `*.util.ts` for pure helpers) mirror the reference template's conventions. |
| [ ] **DoD.8** | Audit fields never returned in API responses. | N/A — no API/backend in this workstream. |
| [x] **DoD.9** | DTOs and data mappings: no shape mismatches, correct casing, correct nullability. | `core/models/domain/job-application.model.ts:1-13` and `core/models/domain/cv-attachment.model.ts:1-9` match the Plan §4 client-side persisted shape (`fullName`, `email`, `coverNote?`, `cvFileName`, `cvFileSize`, `cvFileType`, `submittedAt`) exactly; `application-form.component.ts:90-97` builds the exact same field names before the page appends `submittedAt` (`job-application-page.component.ts:33`). |

## Security (shift-left enforcement of the Plan's threat model)
| # | Item | Evidence |
|---|---|---|
| [x] **DoD.10** | Every mitigation listed in Plan §6 (Threat Model) has been implemented in code; file:line evidence recorded per row. | **Tampering:** CV type/size enforced client-side before acceptance, never trusting the `accept` attribute alone — `core/utils/cv-file-validation.util.ts:12-39`, invoked from `cv-upload.component.ts:78-93` before any state write. **Information Disclosure:** all candidate text rendered via Angular interpolation only, no `[innerHTML]`/`bypassSecurityTrust*` anywhere in `src/app` (verified via repo-wide grep — zero matches); CV file bytes never read into state, only `name`/`size`/`type` (`core/utils/cv-file-validation.util.ts:31-38`); no `console.log`/PII logging anywhere in `src/app` (verified via grep — zero matches). **Denial of Service:** cover note capped to 500 chars before it reaches state (`core/utils/application-form-validators.util.ts:28-29`, invoked at `application-form.component.ts:71`); CV size/type capped before acceptance (`cv-file-validation.util.ts:22-39`); `setItem`/`getItem` exceptions are the expected, handled `StorageUnavailableError` path, never an unhandled crash (`job-application-storage.service.ts:20-24,32-37`, caught by `job-application-page.component.ts:35-43`). **Spoofing/Repudiation/Elevation of Privilege:** N/A per Plan §6 — no identity, audit trail, or privileged action exists in this feature; no code introduces any. |
| [ ] **DoD.11** | All endpoints have explicit `[Authorize]` (or anonymous-by-design note). | N/A — no API endpoints in this workstream. |
| [ ] **DoD.12** | All tenant-scoped queries/commands are scoped by `OrganizationId`. | N/A — no backend/tenant concept; this is an anonymous, single-browser client feature (User Story §5). |
| [x] **DoD.13** | Input validation present on every user-entered field. | Full name required (`application-form-validators.util.ts:6-8`), email required + format (`application-form-validators.util.ts:11-19`), CV required (`cv-upload.component.ts:26-40` `hasError`/`hint` computeds), cover note capped at 500 chars (`application-form-validators.util.ts:28-29`), CV type/size capped (`cv-file-validation.util.ts:22-39`). |
| [x] **DoD.14** | No secrets / API keys / connection strings committed to source. | Repo-wide case-insensitive grep for `api_key|secret|password|token|connectionstring` literal assignments under `frontend-app/hireflow-web/src` returned zero matches. |
| [ ] **DoD.15** | No raw SQL built via string concatenation with user input. | N/A — no database/SQL in this workstream. |
| [x] **DoD.16** | Logs do not contain secrets, tokens, passwords, or PII. | No `console.*` calls exist anywhere in `src/app` (verified via grep — zero matches); Plan §8 explicitly forbids PII in any diagnostic logging and none was added. |
| [x] **DoD.17** | Frontend does not use `bypassSecurityTrust*` on untrusted data and does not bind `[innerHTML]` to server data without sanitisation. | Repo-wide grep for `innerHTML` and `bypassSecurityTrust` under `frontend-app/hireflow-web/src` returned zero matches; all candidate-entered text (`fullName`, `email`, `coverNote`, CV metadata) is rendered exclusively via Angular interpolation (`{{ }}`) in `application-form.component.html`, `application-confirmation.component.html`, and `cv-upload.component.html`. |

## Automated gates (must all be PASS — paste the output / log location)
| # | Item | Evidence |
|---|---|---|
| [x] **DoD.18** | Build passes: `ng build`. | `npx ng build` in `frontend-app/hireflow-web` completed with "Application bundle generation complete" and zero errors (run 2026-08-13, initial bundle 243.96 kB / 65.86 kB transfer, budgets within `angular.json` production limits). |
| [x] **DoD.19** | Tests pass: `ng test --watch=false`. | `npx ng test --watch=false` in `frontend-app/hireflow-web`: **10 test files passed, 44 tests passed, 0 failed** (Vitest via `@angular/build:unit-test`, run 2026-08-13). |
| [x] **DoD.20** | Lint clean: `ng lint`. | Added `@angular-eslint/schematics` to the new workspace (`ng add @angular-eslint/schematics`, wired into `angular.json` `architect.lint`); `npx ng lint` → "All files pass linting." (run 2026-08-13). |
| [x] **DoD.21** | Dependency audit clean of High/Critical CVEs. | `npm audit --production` in `frontend-app/hireflow-web` → "found 0 vulnerabilities" (run 2026-08-13). |
| [x] **DoD.22** | No new secrets detected. | `gitleaks` not installed in this environment; manually grepped `frontend-app/hireflow-web/src` for common secret patterns (API key/secret/password/token/connection string literal assignments) — zero matches. |
| [ ] **DoD.23** | EF migrations applied/rolled back cleanly. | N/A — no database/EF migrations in this workstream. |

## Testing
| # | Item | Evidence |
|---|---|---|
| [x] **DoD.24** | Unit tests cover the branching logic added (services, validators, utils). | `core/utils/cv-file-validation.util.spec.ts` (boundary at exactly 10MB, over-limit, disallowed type, size formatting); `core/utils/application-form-validators.util.spec.ts` (missing/whitespace name, missing/malformed/valid email, note truncation); `core/services/job-application-storage.service.spec.ts` (round-trip, accumulation, `setItem`/`getItem` throw → `StorageUnavailableError`); `shared/utils/dead-link.util.spec.ts`. |
| [ ] **DoD.25** | Integration tests cover each new endpoint (unauthenticated, wrong tenant, wrong role, invalid input, oversized input). | N/A — no endpoints in this workstream. |
| [x] **DoD.26** | Frontend tests cover component logic and at least one AC per user-visible flow. | `job-posting-details.component.spec.ts` (AC-1); `cv-upload.component.spec.ts` (AC-4, AC-5); `application-form.component.spec.ts` (AC-3, AC-6, AC-7, AC-8); `application-confirmation.component.spec.ts` (AC-2, AC-8, AC-9); `job-application-page.component.spec.ts` (AC-2, AC-8, AC-9, AC-10). All 44 tests pass (see DoD.19). |

## Observability
| # | Item | Evidence |
|---|---|---|
| [x] **DoD.27** | Structured log events emitted for privileged/security-sensitive actions with correlation id. | N/A per Plan §8 — no backend/telemetry pipeline exists in this phase; no `console.*`/logging was added, consistent with the Plan's instruction not to wire any network call or add unnecessary diagnostic logging. |
| [x] **DoD.28** | No noisy logs added at Information level for hot paths. | Confirmed zero `console.*` calls anywhere in `frontend-app/hireflow-web/src/app` via grep. |

## API contract
| # | Item | Evidence |
|---|---|---|
| [ ] **DoD.29** | `docs/api-docs/<N>_<FEATURE>_API_DOC.md` updated and matches the implementation exactly. | N/A — Plan §5 confirms this story has no API and no `API_DOC` is produced (PRD Non-Goal NG1). |

## Handoff
| # | Item | Evidence |
|---|---|---|
| [x] **DoD.30** | Story Status set to `Ready for Review`. | See `docs/user-stories/1.1_JOB_APPLICATION_FRONTEND_USER_STORY.md` — Completion Notes and Status updated in this iteration. |
| [x] **DoD.31** | Team Lead notified so it can invoke the Code Reviewer. | Reported in this handoff summary to the orchestrating agent (Sanjeewa / Team Lead). |

## Developer Sign-off
- **Backend:** N/A — no backend tasks in this workstream.
- **Frontend:** Hasika — 2026-08-13
- **Unticked items (if any):** DoD.5, DoD.8, DoD.11, DoD.12, DoD.15, DoD.23, DoD.25, DoD.29 are all **N/A** (no backend/API/database exists in this frontend-only, localStorage-only workstream per PRD Non-Goal NG1 and Plan §§3.1/3.2/5/10/11) — left unticked to signal "not applicable" rather than false-ticking a check that has no target in this story; no blocking gap.
